# Copy to .env and fill in. .env is loaded automatically in dev (see config/runtime.exs).
# Non-empty shell environment variables take precedence over .env.
SERPER_API_KEY=
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4.1-mini
